services:
  n8n:
    image: docker.n8n.io/n8nio/n8n
    ports:
      - "5678:5678"
    volumes:
      - n8n_data:/home/node/.n8n
    depends_on:
      - imap-service

  imap-service:
    build: ./imap-service
    ports:
      - "5000:5000"
    env_file:
      - .env

volumes:
  n8n_data: